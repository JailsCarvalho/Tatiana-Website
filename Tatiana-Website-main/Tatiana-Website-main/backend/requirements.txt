# Dependências de runtime — é este ficheiro que a Vercel instala para a
# função do backend. Ferramentas de desenvolvimento (uvicorn, alembic,
# pytest, linters) ficam em requirements-dev.txt, para não engordar o
# bundle de produção. Localmente: pip install -r requirements-dev.txt
# (já inclui este ficheiro via -r requirements.txt).
fastapi==0.110.1
httpx>=0.27.0
python-dotenv>=1.0.1
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
tzdata>=2024.2
python-multipart>=0.0.9

# Vercel Blob Storage — só entra em ação quando BLOB_READ_WRITE_TOKEN existe
# (produção na Vercel); em desenvolvimento local os uploads vão para disco
# e este pacote fica sem uso. Ver backend/storage.py.
vercel
